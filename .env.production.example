# Production environment variables
# 保持 HOST 为 0.0.0.0，避免服务器主机名解析失败
HOST=0.0.0.0
PORT=3000
# SQLite 默认示例；如使用 MySQL/Postgres 请替换
DATABASE_URL=file:./prisma/dev.db

# SQLite (default for demo); the file is prisma/dev.db relative to schema
DATABASE_URL="file:./dev.db"

# If you switch to MySQL/Postgres, replace DATABASE_URL accordingly.
# MySQL:    mysql://<user>:<password>@<host>:<port>/<db>
# Postgres: postgresql://<user>:<password>@<host>:<port>/<db>?schema=public

# Server port for Next.js production
PORT=3000